<% @sections.each do |section| %>
   <div class="card-container">
     <div class="card">
       <div class="front"><p><%= section.title %></p></div>
       <div class="back">Back</div>
     </div>
   </div>
<% end %>
